package com.example.controllers;

/**
 * This is a controller for the load game page. It uses the ViewManager to request changes based on button click actions taken in its respective view (FXML file).
 */
public class LoadGameController extends Controller {
}
