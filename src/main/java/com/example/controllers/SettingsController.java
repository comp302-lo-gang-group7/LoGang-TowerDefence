package com.example.controllers;

import com.example.main.Main;
import javafx.fxml.FXML;

/**
 * This is a controller for the settings page. It uses the ViewManager to request changes based on button click actions taken in its respective view (FXML file).
 */
public class SettingsController extends Controller {

}
